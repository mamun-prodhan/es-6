const numbers = [45, 20, 25];
const student = {
    name: 'Mamun Prodhan',
    age: 5
}

const result = `The sum of ${numbers[1]} and ${student.age} is = ${numbers[1] + student.age}`
console.log(result);