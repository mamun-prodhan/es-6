// /* 
// write an arrow function that will take 3 parameters, sill multiply the parameters and will return the result.
// */

// const multiply = (num1, num2, num3) => num1 * num2 * num3;

// console.log(multiply(3,3,3));
const obj = { foo: 1 };
obj.bar = 2;
console.log(obj);